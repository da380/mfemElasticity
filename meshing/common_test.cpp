#include "common.hpp"

#include <iostream>

int main() {
  //  auto circle1 = Circle{0, 0, 1};

  // auto circle2 = Circle{0, 0, 2};

  // auto circles = Circles({circle1, circle2});

  // std::cout << circles.Small() << std::endl;
  // std::cout << circles.Big() << std::endl;
}